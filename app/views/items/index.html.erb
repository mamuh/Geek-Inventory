<div class="container text-center">
  <h2>What are you buying?</h2>

    <div class="d-flex flex-wrap justify-content-around">
      <% @items.each do |item| %>
          <div class="item-card text-center mt-2 p-2">
            <ul style="list-style: none">
              <% if item.photo.attached? %>
                <li><%= cl_image_tag item.photo.key, crop: :fit, width: 150, height: 150, class: "mt-2" %></li>
              <% end %>
              <div class="bg-light">
                <li><h5><%= item.name %></h5></li>
              </div>
                <li><%= item.description %></li>
              <li><%= item.category.name %></li>
              <li>$ <%= item.price %></li>
                <% if current_user.role == "seller" %>
                  <li><%= link_to "Buy", item_transactions_path(item),method: :post, class: "btn btn-success" %></li>
                <% end %>
              <li><%= link_to "Details", item_path(item), class: "btn btn-secondary" %></li>
            </ul>
          </div>
      <% end %>
    </div>
</div>

